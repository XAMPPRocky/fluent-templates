hello-world = Bonjour le monde!
greeting = Bonjour { $name }!
        .placeholder = Salut l'ami!
simple = texte simple
reference = texte simple avec une référence: { -something }

parameter = texte avec une { $param }
parameter2 = texte une { $param } seconde { $multi-word-param }

email = text avec un EMAIL("example@example.org")

# no fallback
